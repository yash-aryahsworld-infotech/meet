import 'package:flutter/material.dart';

class CompletedConsultationsTab extends StatelessWidget {
  const CompletedConsultationsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("Completed Consultations Tab Content"),
    );
  }
}
