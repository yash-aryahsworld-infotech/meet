import 'package:flutter/material.dart';

class AllConsultationsTab extends StatelessWidget {
  const AllConsultationsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("All Consultations Tab Content"),
    );
  }
}
