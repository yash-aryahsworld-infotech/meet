import 'package:flutter/material.dart';

class ActiveConsultationsTab extends StatelessWidget {
  const ActiveConsultationsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("Active Consultations Tab Content"),
    );
  }
}
