import '../utils/logger.dart';

// This service is not used - we use socket_signaling_service.dart instead
// Keeping as stub to avoid breaking imports
class SignalingService {
  void dispose() {
    Logger.log('SignalingService disposed (stub)');
  }
}
